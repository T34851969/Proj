# Docker 与本地开发指南（索引）

本仓库的 Docker 使用文档已拆分为操作系统专用的子文档：

- Windows（Docker Desktop / 推荐 WSL2）：DOCS/docker-windows.md
- Debian / Ubuntu（Docker Engine + Compose 插件）：DOCS/docker-debian.md

请根据你的开发环境打开对应文档进行操作；完成后如需我运行一次本地 smoke-build（构建镜像并启动服务以检验基本功能），回复“运行 smoke-build”。
